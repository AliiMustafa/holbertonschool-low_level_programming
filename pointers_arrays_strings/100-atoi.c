#include "main.h"

/**
 * _atoi - Main entry
 * @s: pointer
 * Return: takes the integers from the string
 */
int _atoi(char *s)
{
	int i;
	char temp = "0123456789";
	char plus = "+";
	char minus = "-";

	i = 0;
	while (*(s + i) != 0)
	{
		i++;
	}
	m = i - 1;
	i = 0;
	
}
